# coding: utf-8
# Copyright (c) 2016, 2026, Oracle and/or its affiliates.  All rights reserved.
# This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.
# NOTE: This class is auto generated by OracleSDKGenerator. DO NOT EDIT. API Version: 20220901

from __future__ import print_function
import click
import oci  # noqa: F401
import sys  # noqa: F401
from oci_cli import cli_constants  # noqa: F401
from oci_cli import cli_util
from oci_cli import json_skeleton_utils
from oci_cli import custom_types  # noqa: F401
from oci_cli.aliasing import CommandGroupWithAlias
from services.marketplace_private_offer.src.oci_cli_marketplace_private_offer.generated import marketplace_private_offer_service_cli


@click.command(cli_util.override('quote_attachment.quote_attachment_root_group.command_name', 'quote-attachment'), cls=CommandGroupWithAlias, help=cli_util.override('quote_attachment.quote_attachment_root_group.help', """Use the Marketplace Publisher API to manage the publishing of applications in Oracle Cloud Infrastructure Marketplace."""), short_help=cli_util.override('quote_attachment.quote_attachment_root_group.short_help', """MarketplacePrivateOffer API"""))
@cli_util.help_option_group
def quote_attachment_root_group():
    pass


@click.command(cli_util.override('quote_attachment.quote_attachment_collection_group.command_name', 'quote-attachment-collection'), cls=CommandGroupWithAlias, help="""Results of offer quote attachment summaries.""")
@cli_util.help_option_group
def quote_attachment_collection_group():
    pass


@click.command(cli_util.override('quote_attachment.quote_attachment_group.command_name', 'quote-attachment'), cls=CommandGroupWithAlias, help="""Description of an offer quote attachment.""")
@cli_util.help_option_group
def quote_attachment_group():
    pass


marketplace_private_offer_service_cli.marketplace_private_offer_service_group.add_command(quote_attachment_root_group)
quote_attachment_root_group.add_command(quote_attachment_collection_group)
quote_attachment_root_group.add_command(quote_attachment_group)


@quote_attachment_group.command(name=cli_util.override('quote_attachment.create_quote_attachment.command_name', 'create'), help=u"""Creates a new offer quote attachment. \n[Command Reference](createQuoteAttachment)""")
@cli_util.option('--offer-quote-id', required=True, help=u"""Unique offer quote identifier.""")
@cli_util.option('--file-base64-encoded', required=True, help=u"""Base64-encoded file to attach to the offer quote.""")
@cli_util.option('--display-name', required=True, help=u"""The name used to refer to the uploaded data.""")
@cli_util.option('--type', required=True, help=u"""The type of offer quote attachment.""")
@cli_util.option('--recipient', required=True, help=u"""The recipient of the offer quote attachment.""")
@cli_util.option('--wait-for-state', type=custom_types.CliCaseInsensitiveChoice(["CREATING", "UPDATING", "ACTIVE", "DELETING", "DELETED", "FAILED"]), multiple=True, help="""This operation creates, modifies or deletes a resource that has a defined lifecycle state. Specify this option to perform the action and then wait until the resource reaches a given lifecycle state. Multiple states can be specified, returning on the first state. For example, --wait-for-state CREATING --wait-for-state FAILED would return on whichever lifecycle state is reached first. If timeout is reached, a return code of 2 is returned. For any other error, a return code of 1 is returned.""")
@cli_util.option('--max-wait-seconds', type=click.INT, help="""The maximum time to wait for the resource to reach the lifecycle state defined by --wait-for-state. Defaults to 1200 seconds.""")
@cli_util.option('--wait-interval-seconds', type=click.INT, help="""Check every --wait-interval-seconds to see whether the resource has reached the lifecycle state defined by --wait-for-state. Defaults to 30 seconds.""")
@json_skeleton_utils.get_cli_json_input_option({})
@cli_util.help_option
@click.pass_context
@json_skeleton_utils.json_skeleton_generation_handler(input_params_to_complex_types={}, output_type={'module': 'marketplace_private_offer', 'class': 'QuoteAttachment'})
@cli_util.wrap_exceptions
def create_quote_attachment(ctx, from_json, wait_for_state, max_wait_seconds, wait_interval_seconds, offer_quote_id, file_base64_encoded, display_name, type, recipient):

    if isinstance(offer_quote_id, str) and len(offer_quote_id.strip()) == 0:
        raise click.UsageError('Parameter --offer-quote-id cannot be whitespace or empty string')

    kwargs = {}
    kwargs['opc_request_id'] = cli_util.use_or_generate_request_id(ctx.obj['request_id'])

    _details = {}
    _details['fileBase64Encoded'] = file_base64_encoded
    _details['displayName'] = display_name
    _details['type'] = type
    _details['recipient'] = recipient

    client = cli_util.build_client('marketplace_private_offer', 'quote_attachment', ctx)
    result = client.create_quote_attachment(
        offer_quote_id=offer_quote_id,
        create_quote_attachment_details=_details,
        **kwargs
    )
    if wait_for_state:

        if hasattr(client, 'get_quote_attachment') and callable(getattr(client, 'get_quote_attachment')):
            try:
                wait_period_kwargs = {}
                if max_wait_seconds is not None:
                    wait_period_kwargs['max_wait_seconds'] = max_wait_seconds
                if wait_interval_seconds is not None:
                    wait_period_kwargs['max_interval_seconds'] = wait_interval_seconds

                click.echo('Action completed. Waiting until the resource has entered state: {}'.format(wait_for_state), file=sys.stderr)
                result = oci.wait_until(client, client.get_quote_attachment(result.data.id), 'lifecycle_state', wait_for_state, **wait_period_kwargs)
            except oci.exceptions.MaximumWaitTimeExceeded as e:
                # If we fail, we should show an error, but we should still provide the information to the customer
                click.echo('Failed to wait until the resource entered the specified state. Outputting last known resource state', file=sys.stderr)
                cli_util.render_response(result, ctx)
                sys.exit(2)
            except Exception:
                click.echo('Encountered error while waiting for resource to enter the specified state. Outputting last known resource state', file=sys.stderr)
                cli_util.render_response(result, ctx)
                raise
        else:
            click.echo('Unable to wait for the resource to enter the specified state', file=sys.stderr)
    cli_util.render_response(result, ctx)


@quote_attachment_group.command(name=cli_util.override('quote_attachment.delete_quote_attachment.command_name', 'delete'), help=u"""Deletes an offer quote attachment resource by identifier. \n[Command Reference](deleteQuoteAttachment)""")
@cli_util.option('--offer-quote-id', required=True, help=u"""Unique offer quote identifier.""")
@cli_util.option('--quote-attachment-id', required=True, help=u"""Unique offer quote attachment identifier.""")
@cli_util.option('--if-match', help=u"""For optimistic concurrency control. In the PUT or DELETE call for a resource, set the `if-match` parameter to the value of the etag from a previous GET or POST response for that resource. The resource will be updated or deleted only if the etag you provide matches the resource's current etag value.""")
@cli_util.confirm_delete_option
@json_skeleton_utils.get_cli_json_input_option({})
@cli_util.help_option
@click.pass_context
@json_skeleton_utils.json_skeleton_generation_handler(input_params_to_complex_types={})
@cli_util.wrap_exceptions
def delete_quote_attachment(ctx, from_json, offer_quote_id, quote_attachment_id, if_match):

    if isinstance(offer_quote_id, str) and len(offer_quote_id.strip()) == 0:
        raise click.UsageError('Parameter --offer-quote-id cannot be whitespace or empty string')

    if isinstance(quote_attachment_id, str) and len(quote_attachment_id.strip()) == 0:
        raise click.UsageError('Parameter --quote-attachment-id cannot be whitespace or empty string')

    kwargs = {}
    if if_match is not None:
        kwargs['if_match'] = if_match
    kwargs['opc_request_id'] = cli_util.use_or_generate_request_id(ctx.obj['request_id'])
    client = cli_util.build_client('marketplace_private_offer', 'quote_attachment', ctx)
    result = client.delete_quote_attachment(
        offer_quote_id=offer_quote_id,
        quote_attachment_id=quote_attachment_id,
        **kwargs
    )
    cli_util.render_response(result, ctx)


@quote_attachment_group.command(name=cli_util.override('quote_attachment.get_quote_attachment.command_name', 'get'), help=u"""Gets an offer quote attachment by identifier. \n[Command Reference](getQuoteAttachment)""")
@cli_util.option('--offer-quote-id', required=True, help=u"""Unique offer quote identifier.""")
@cli_util.option('--quote-attachment-id', required=True, help=u"""Unique offer quote attachment identifier.""")
@json_skeleton_utils.get_cli_json_input_option({})
@cli_util.help_option
@click.pass_context
@json_skeleton_utils.json_skeleton_generation_handler(input_params_to_complex_types={}, output_type={'module': 'marketplace_private_offer', 'class': 'QuoteAttachment'})
@cli_util.wrap_exceptions
def get_quote_attachment(ctx, from_json, offer_quote_id, quote_attachment_id):

    if isinstance(offer_quote_id, str) and len(offer_quote_id.strip()) == 0:
        raise click.UsageError('Parameter --offer-quote-id cannot be whitespace or empty string')

    if isinstance(quote_attachment_id, str) and len(quote_attachment_id.strip()) == 0:
        raise click.UsageError('Parameter --quote-attachment-id cannot be whitespace or empty string')

    kwargs = {}
    kwargs['opc_request_id'] = cli_util.use_or_generate_request_id(ctx.obj['request_id'])
    client = cli_util.build_client('marketplace_private_offer', 'quote_attachment', ctx)
    result = client.get_quote_attachment(
        offer_quote_id=offer_quote_id,
        quote_attachment_id=quote_attachment_id,
        **kwargs
    )
    cli_util.render_response(result, ctx)


@quote_attachment_group.command(name=cli_util.override('quote_attachment.get_quote_attachment_content.command_name', 'get-quote-attachment-content'), help=u"""Gets an offer quote attachment content by identifier. \n[Command Reference](getQuoteAttachmentContent)""")
@cli_util.option('--offer-quote-id', required=True, help=u"""Unique offer quote identifier.""")
@cli_util.option('--quote-attachment-id', required=True, help=u"""Unique offer quote attachment identifier.""")
@cli_util.option('--file', type=click.File(mode='wb'), required=True, help="The name of the file that will receive the response data, or '-' to write to STDOUT.")
@json_skeleton_utils.get_cli_json_input_option({})
@cli_util.help_option
@click.pass_context
@json_skeleton_utils.json_skeleton_generation_handler(input_params_to_complex_types={})
@cli_util.wrap_exceptions
def get_quote_attachment_content(ctx, from_json, file, offer_quote_id, quote_attachment_id):

    if isinstance(offer_quote_id, str) and len(offer_quote_id.strip()) == 0:
        raise click.UsageError('Parameter --offer-quote-id cannot be whitespace or empty string')

    if isinstance(quote_attachment_id, str) and len(quote_attachment_id.strip()) == 0:
        raise click.UsageError('Parameter --quote-attachment-id cannot be whitespace or empty string')

    kwargs = {}
    kwargs['opc_request_id'] = cli_util.use_or_generate_request_id(ctx.obj['request_id'])
    client = cli_util.build_client('marketplace_private_offer', 'quote_attachment', ctx)
    result = client.get_quote_attachment_content(
        offer_quote_id=offer_quote_id,
        quote_attachment_id=quote_attachment_id,
        **kwargs
    )

    # If outputting to stdout we don't want to print a progress bar because it will get mixed up with the output
    # Also we need a non-zero Content-Length in order to display a meaningful progress bar
    bar = None
    if hasattr(file, 'name') and file.name != '<stdout>' and 'Content-Length' in result.headers:
        content_length = int(result.headers['Content-Length'])
        if content_length > 0:
            bar = click.progressbar(length=content_length, label='Downloading file')

    try:
        if bar:
            bar.__enter__()

        # TODO: Make the download size a configurable option
        # use decode_content=True to automatically unzip service responses (this should be overridden for object storage)
        for chunk in result.data.raw.stream(cli_constants.MEBIBYTE, decode_content=True):
            if bar:
                bar.update(len(chunk))
            file.write(chunk)
    finally:
        if bar:
            bar.render_finish()
        file.close()


@quote_attachment_collection_group.command(name=cli_util.override('quote_attachment.list_quote_attachments.command_name', 'list-quote-attachments'), help=u"""Returns a list of offer quote attachments. Requires either a reseller compartment ID or an ISV compartment ID. \n[Command Reference](listQuoteAttachments)""")
@cli_util.option('--offer-quote-id', required=True, help=u"""Unique offer quote identifier.""")
@cli_util.option('--reseller-compartment-id', help=u"""OCID of the reseller's compartment.""")
@cli_util.option('--isv-compartment-id', help=u"""OCID of the ISV's compartment.""")
@cli_util.option('--lifecycle-state', type=custom_types.CliCaseInsensitiveChoice(["CREATING", "UPDATING", "ACTIVE", "DELETING", "DELETED", "FAILED"]), help=u"""A filter to return only resources their lifecycleState matches the given lifecycleState.""")
@cli_util.option('--display-name', help=u"""A filter to return only resources that match the entire display name given.""")
@cli_util.option('--id', help=u"""Unique offer quote attachment identifier.""")
@cli_util.option('--limit', type=click.INT, help=u"""The maximum number of items to return.""")
@cli_util.option('--page', help=u"""A token representing the position at which to start retrieving results. This must come from the `opc-next-page` header field of a previous response.""")
@cli_util.option('--sort-order', type=custom_types.CliCaseInsensitiveChoice(["ASC", "DESC"]), help=u"""The sort order to use, either 'ASC' or 'DESC'.""")
@cli_util.option('--sort-by', type=custom_types.CliCaseInsensitiveChoice(["timeCreated", "displayName"]), help=u"""The field to sort by. Only one sort order may be provided. Default order for timeCreated is descending. Default order for displayName is ascending.""")
@cli_util.option('--all', 'all_pages', is_flag=True, help="""Fetches all pages of results. If you provide this option, then you cannot provide the --limit option.""")
@cli_util.option('--page-size', type=click.INT, help="""When fetching results, the number of results to fetch per call. Only valid when used with --all or --limit, and ignored otherwise.""")
@json_skeleton_utils.get_cli_json_input_option({})
@cli_util.help_option
@click.pass_context
@json_skeleton_utils.json_skeleton_generation_handler(input_params_to_complex_types={}, output_type={'module': 'marketplace_private_offer', 'class': 'QuoteAttachmentCollection'})
@cli_util.wrap_exceptions
def list_quote_attachments(ctx, from_json, all_pages, page_size, offer_quote_id, reseller_compartment_id, isv_compartment_id, lifecycle_state, display_name, id, limit, page, sort_order, sort_by):

    if all_pages and limit:
        raise click.UsageError('If you provide the --all option you cannot provide the --limit option')

    if isinstance(offer_quote_id, str) and len(offer_quote_id.strip()) == 0:
        raise click.UsageError('Parameter --offer-quote-id cannot be whitespace or empty string')

    kwargs = {}
    if reseller_compartment_id is not None:
        kwargs['reseller_compartment_id'] = reseller_compartment_id
    if isv_compartment_id is not None:
        kwargs['isv_compartment_id'] = isv_compartment_id
    if lifecycle_state is not None:
        kwargs['lifecycle_state'] = lifecycle_state
    if display_name is not None:
        kwargs['display_name'] = display_name
    if id is not None:
        kwargs['id'] = id
    if limit is not None:
        kwargs['limit'] = limit
    if page is not None:
        kwargs['page'] = page
    if sort_order is not None:
        kwargs['sort_order'] = sort_order
    if sort_by is not None:
        kwargs['sort_by'] = sort_by
    kwargs['opc_request_id'] = cli_util.use_or_generate_request_id(ctx.obj['request_id'])
    client = cli_util.build_client('marketplace_private_offer', 'quote_attachment', ctx)
    if all_pages:
        if page_size:
            kwargs['limit'] = page_size

        result = cli_util.list_call_get_all_results(
            client.list_quote_attachments,
            offer_quote_id=offer_quote_id,
            **kwargs
        )
    elif limit is not None:
        result = cli_util.list_call_get_up_to_limit(
            client.list_quote_attachments,
            limit,
            page_size,
            offer_quote_id=offer_quote_id,
            **kwargs
        )
    else:
        result = client.list_quote_attachments(
            offer_quote_id=offer_quote_id,
            **kwargs
        )
    cli_util.render_response(result, ctx)
